= wrap_layout :layout do

  = partial :header, locals: { title: current_article.title, subtitle: current_article.data.subtitle }

  %article
    .container
      .row
        .col-lg-8.col-lg-offset-2.col-md-10.col-md-offset-1
          = yield
          %hr/

